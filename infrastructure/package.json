{
  "name": "infrastructure",
  "version": "0.1.0",
  "bin": {
    "infrastructure": "bin/prepg3-stack.js"
  },
  "lib": "lib",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "cdk deploy --all --context environment=live",
    "destroy": "cdk destroy --all --context environment=live",
    "diff": "cdk diff --all --context environment=live",
    "synth": "cdk synth --all --context environment=live",
    "export-config": "node ../scripts/export-aws-config.js live"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^24.10.1",
    "aws-cdk": "2.1100.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "@aws-sdk/client-cloudformation": "^3.958.0",
    "aws-cdk-lib": "2.232.1",
    "source-map-support": "^0.5.21",
    "constructs": "^10.0.0"
  }
}
